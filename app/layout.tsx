import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dhrati Sharma — Senior Software Test Engineer",
  description:
    "Dhrati Sharma is a Senior Software Test Engineer at Visa, focused on quality engineering, test automation, and dependable software delivery.",
  openGraph: {
    title: "Dhrati Sharma — Senior Software Test Engineer",
    description:
      "Quality-focused software engineer building confidence into complex systems.",
    type: "profile",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
